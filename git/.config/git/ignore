### Linux ###
*~

# temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*

# KDE directory preferences
.directory

# Linux trash folder which might appear on any partition or disk
.Trash-*


### OSX ###
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Created by https://www.gitignore.io/api/emacs

## Custom additions
# artifacts from git merge conflicts
*.orig
# Autoenv
.autoenv.zsh
.tool-versions

# Claude local overrides and worktrees
**/.claude/settings.local.json
.claude/worktrees/

# vscode settings are private unless explicitly shared
.vscode/
# Mise is ignored unless explicitly un-ignored
mise.toml
